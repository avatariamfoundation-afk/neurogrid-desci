# Release Tagging Procedure

1. Ensure main branch is clean
2. Run:
   git tag -a v0.1.0-testnet -m "BNB Testnet deployment"
3. Push tag:
   git push origin v0.1.0-testnet

Tags are immutable once published.

